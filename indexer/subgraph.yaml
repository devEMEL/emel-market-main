specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: FHEEmelMarket
    network: sepolia
    source:
      address: "0xa592C283b5d389E2e52d5363320448E95B75b838" 
      abi: FHEEmelMarket
      startBlock: 9025909
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - Auction
      abis:
        - name: FHEEmelMarket
          file: ./abis/FHEEmelMarket.json
      eventHandlers:
        - event: AuctionCancelled(indexed uint256)
          handler: handleAuctionCancelled
        - event: AuctionCreated(indexed uint256,indexed address,uint256,indexed address,uint256,uint256)
          handler: handleAuctionCreated
        - event: BidPlaced(indexed uint256)
          handler: handleBidPlaced
        - event: NFTClaimed(indexed uint256)
          handler: handleNFTClaimed
        - event: AuctionResolved(indexed uint256)
          handler: handleAuctionResolved
      file: ./src/fhe-emel-market.ts

